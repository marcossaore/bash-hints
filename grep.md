## Grep

```bash	
    cat file.txt | grep word # search the word in the file
    cat file.txt | grep -v word # exclude the word in the file
    cat file.txt | grep -i word # case insensitive
    grep --help # help
